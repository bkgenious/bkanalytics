# Portfolio Platform Environment Variables
# ==========================================
# Copy this file to .env.local and configure for your environment
#
# GITHUB CODESPACES USERS:
# - This file is automatically copied to .env.local on first launch
# - Update .env.local with secure credentials before going live
# ==========================================

# ============================================
# ADMIN AUTHENTICATION (REQUIRED)
# ============================================

# Admin username for /admin panel access
# Recommendation: Use something other than 'admin'
ADMIN_USERNAME=admin

# Admin password - MUST be strong in production
# Minimum: 16 characters with mixed case, numbers, symbols
# Generate: openssl rand -base64 24
ADMIN_PASSWORD=Portfolio@2024

# ============================================
# SESSION SECURITY (REQUIRED)
# ============================================

# Session signing secret - MUST be unique and random
# Generate: openssl rand -hex 32
# NEVER share this value or commit it to version control
SESSION_SECRET=change_this_to_a_random_64_character_string_in_production

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Force production mode (usually set by hosting platform)
# NODE_ENV=production

# ============================================
# CODESPACES-SPECIFIC NOTES
# ============================================
# 
# 1. Files uploaded via admin panel exist only in this Codespace
# 2. Uncommitted files are lost when the Codespace is deleted
# 3. To persist uploads, commit them to Git:
#    git add public/uploads/
#    git commit -m "Add uploaded files"
#    git push
#
# 4. The preview URL is temporary and changes each session
# 5. For permanent hosting, deploy to Vercel/Netlify
